Place images exported from your LaTeX source here, using PNG filenames (convert PDFs to PNG/SVG locally and then add them here).

Suggested filenames:

- nn_scaling.png
- nn_snapshot.png
- nn_algorithm_diagram.png

Notes:
- SVG is preferred for vector plots on GitHub; PNG is fine for rasterized images.
- Keep image file sizes reasonable for the web (e.g., 200–600 KB when possible).
